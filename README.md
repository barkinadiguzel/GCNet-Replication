# GCNet-Replication
GCNet-Replication is a minimal implementation of the simplified non-local (global context) module proposed in GCNet, focusing on structure rather than training. It demonstrates how global context is aggregated from spatial attention and added back to features to model long-range dependencies.
